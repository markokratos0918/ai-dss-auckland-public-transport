# Layer 2: AI-DSS Modeling / SHAP

Architecture position:

```text
GTFS + Weather -> AI-DSS Modeling / SHAP -> Decision Engine
```

## Purpose

This layer evaluates the AI-DSS modeling checkpoint using the real Auckland decision output generated by Notebook 09.

The checkpoint supports:

- XGBoost classification for actionable delay risk.
- XGBoost regression for delay minutes.
- Baseline comparisons.
- ARIMA hourly delay baseline.
- SHAP explainability or model-native feature importance.

## Required Local Input

```text
data/processed/decision_engine_output.csv
```

This is a large local Notebook 09 output and should not be committed.

## Commands

Run the full modeling checkpoint:

```bash
python src/train_ai_dss_model.py
```

Run a no-write sanity check:

```bash
python src/train_ai_dss_model.py --dry-run
```

## Expected Small Outputs

```text
data/processed/summaries/ai_model_metrics.csv
data/processed/summaries/ai_feature_importance.csv
data/processed/summaries/ai_prediction_sample.csv
```

## Assessment 1 Alignment

This layer supports:

- AI prediction using route, time, and weather features.
- SHAP explainability or fallback model-native feature importance.
- Baseline comparison for model evaluation.
- Downstream Decision Engine readiness.

## Scope Limits

- The current AI model is a prototype checkpoint, not a deployed live predictor.
- The model is evaluated on the current 21/22-day baseline.
- Rerun the model after the final 30-day dataset is complete.
- Recommendations remain decision-support evidence, not automated operational control.
